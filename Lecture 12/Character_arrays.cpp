#include<iostream>
using  namespace std;

int main() {
	int n;
	cin >> n;
	// int a[] = {1, 2, 3, 4, 5};

	// cout << a << endl;
	// cout << &a[1] << endl;


	//char ch[n] //= {'m', 'a', 'y', 'a', 'n', 'k'};
	//char ch[n] //= {"mayank"};
	// for (int i = 0; i < n; i++) {
	// 	cin >> ch[i];
	// }
	cin >> ch;
	cout << ch << endl;

	for (int i = 0; i < 6; i++) {
		cout << ch[i] << " ";
	}
}